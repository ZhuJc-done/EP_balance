# Synthetic rank-dynamics placeholders

Files in this directory are deterministic synthetic traces for figure-layout
prototyping only. They are not measured routing results and must not be used as
experimental evidence.

Regenerate them with:

```bash
python eval/generate_placeholder_rank_dynamics.py \
  --output-dir eval/placeholder_data
```
